# ── Required ──
DATABASE_URL=postgresql://user:password@localhost:5432/surgil_db
SECRET_KEY=change_me_random_string

# ── Optional ──
APP_TITLE=СУРГИЛ · Dashboard
TZ=Asia/Tashkent

# ── MCP (Claude Code) ──
# Read-only PG URL for MCP postgres server (falls back to DATABASE_URL)
# Recommended: CREATE ROLE mcp_ro LOGIN PASSWORD '...' IN ROLE pg_read_all_data;
# POSTGRES_URL_RO=postgresql://mcp_ro:password@localhost:5432/surgil_db

# Write-access PG URL — used ONLY with explicit permission
# POSTGRES_URL_RW=postgresql://user:password@localhost:5432/surgil_db

# ── Notifications (optional) ──
TELEGRAM_BOT_TOKEN=
TELEGRAM_DEFAULT_CHAT_ID=

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=

# ── Background Jobs ──
JOB_API_SECRET=change_me_job_secret
